syntax = "proto3";
package protocol;

//import "google/protobuf/any.proto";
//import "github.com/gogo/protobuf/protobuf/google/protobuf/any.proto";

message Any {

    //Id uint16 `protobuf:"-" json:"-"`
    uint32 message_id = 1; //message id

    string session_id = 2; //session id

    string type_url = 3; //json request

    // Must be a valid serialized protocol buffer of the above specified type.
    bytes value = 4;
}

//rpc 服务间转发
service RPCService {
    //rpc request (google.protobuf.Any) returns (google.protobuf.Any) {}
    rpc request (Any) returns (Any) {}
}
